{
  "name": "ssst",
  "version": "0.4.1",
  "description": "A web-based image labeling tool (PWA) and BLE debugging CLI",
  "private": true,
  "workspaces": ["apps/*"],
  "license": "MIT",
  "scripts": {
    "dev:web": "bun run --filter web dev",
    "build:web": "bun run --filter web build",
    "build:ssst-cli": "cd apps/ssst-cli && cargo build --release"
  }
}
