{
  "name": "ssst",
  "version": "0.4.1",
  "description": "A Tauri v2 desktop toolbox with image labeling and BLE debugging",
  "private": true,
  "workspaces": ["apps/*"],
  "license": "MIT",
  "scripts": {
    "dev": "bun run --filter desktop dev",
    "build": "bun run --filter desktop build",
    "build:desktop": "bun run --filter desktop build:desktop"
  }
}
